目标是在ulua中加入热修复的功能。使用lua代码替代c#函数。
当前完成情况：
	1、返回是引用类型，目前测试是可以的
	2、返回是值类型的，只能double。因为lua里的数值类型只有double。如果返回其它的类型，需要做类型转换，这块todo。
	3、返回是泛型的未测试

1\根据类的Attribute，修改类中函数的代码，在起始处加入TestChangeMethod.InjectMethod
2\把函数都改造为testCall的方式，如果有lua代码，就调用。否者直接使用旧的
3、修改il。目前尝试了手写和拷贝testCall的方式，都没成功。
	主要注意的是C#的box和unbox这些操作比较烦。之后再做尝试。
4、xlua的修复方式是做一个空delegate来在init的时候赋值，这里的做法是每次查。虽然没有xlua效率高，但是灵活性更强。
5、各种需要传入lua的类型的导入问题（todo)

目前状态，可以使用，但是没有测试过很多种类型。只简单测试过引用类型。